function solution(n) {
    let sqrt = Math.sqrt(n);
    var answer = 0;
    
    if(sqrt == 'NaN' || sqrt % 1 !== 0){
        answer = -1;        
    } else{
        answer = Math.pow(sqrt+1,2);
    }
    
    return answer;
}

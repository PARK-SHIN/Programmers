function solution(numbers) {
    var answer = 45;
  
    for(var j = 0; j < numbers.length; j++){
        
        for(var i = 0; i<10; i++){
            if(numbers[j] == i) answer = answer - i;
        }
    }
    
    return answer;
}


// function solution(numbers) {
//     let answer = 0;

//     for(let i = 0; i <= 9; i++) {
//         if(!numbers.includes(i)) answer += i;
//     }

//     return answer;
// }
